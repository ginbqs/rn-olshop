import {SHORTCUTS} from "../../data/dummy-data"

const initalState = {
    shortcuts: SHORTCUTS,
}

export default shortcutsReducers = (state=initalState,action) => {
    switch(action.type){
        default:
            return state
    }
}