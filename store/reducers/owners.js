import {OWNERS} from "../../data/dummy-data"

const initalState = {
    owners: OWNERS,
}

export default ownersReducers = (state=initalState,action) => {
    switch(action.type){
        default:
            return state
    }
}