import {BANNERS} from "../../data/dummy-data"

const initalState = {
    banners: BANNERS
}

export default bannersReducers = (state=initalState,action) => {
    switch(action.type){
        default:
            return state
    }
}